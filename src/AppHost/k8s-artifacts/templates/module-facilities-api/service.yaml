---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "module-facilities-api-service"
  labels:
    app.kubernetes.io/name: "velotime-apphost"
    app.kubernetes.io/component: "module-facilities-api"
    app.kubernetes.io/instance: "{{.Release.Name}}"
spec:
  type: "ClusterIP"
  selector:
    app.kubernetes.io/name: "velotime-apphost"
    app.kubernetes.io/component: "module-facilities-api"
    app.kubernetes.io/instance: "{{.Release.Name}}"
  ports:
    - name: "http"
      protocol: "TCP"
      port: "{{ .Values.parameters.module_facilities_api.port_http }}"
      targetPort: "{{ .Values.parameters.module_facilities_api.port_http }}"
    - name: "https"
      protocol: "TCP"
      port: "{{ .Values.parameters.module_facilities_api.port_https }}"
      targetPort: "{{ .Values.parameters.module_facilities_api.port_https }}"
