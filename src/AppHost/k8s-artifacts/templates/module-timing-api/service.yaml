---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "module-timing-api-service"
  labels:
    app.kubernetes.io/name: "velotime-apphost"
    app.kubernetes.io/component: "module-timing-api"
    app.kubernetes.io/instance: "{{.Release.Name}}"
spec:
  type: "ClusterIP"
  selector:
    app.kubernetes.io/name: "velotime-apphost"
    app.kubernetes.io/component: "module-timing-api"
    app.kubernetes.io/instance: "{{.Release.Name}}"
  ports:
    - name: "http"
      protocol: "TCP"
      port: "{{ .Values.parameters.module_timing_api.port_http }}"
      targetPort: "{{ .Values.parameters.module_timing_api.port_http }}"
    - name: "https"
      protocol: "TCP"
      port: "{{ .Values.parameters.module_timing_api.port_https }}"
      targetPort: "{{ .Values.parameters.module_timing_api.port_https }}"
